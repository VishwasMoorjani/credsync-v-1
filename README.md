# Client Helper - Local Notes (Version 1)

### 📌 Overview
This version stores notes locally in your browser using `chrome.storage.local`.  
It is completely offline, private, and ideal for quick, per-domain reminders.

---

### ⚙️ Features
- Save text notes for each website (domain-based).
- Auto-load notes when you revisit the same site.
- Works fully offline.
- Zero setup — no Google Sheets or external APIs.

---

### 🧩 Installation
1. Open `chrome://extensions`
2. Enable **Developer Mode**
3. Click **Load unpacked**
4. Select this folder (`v1_local_notes`)
5. Visit any site and click the 🔐 button to open the sidebar.

---

### 💾 Storage
Notes are saved in Chrome’s local storage under the domain name key.  
They are **not shared** between browsers or machines.

---

### 🧠 Ideal Use
Use this when you want a simple private sticky-note system inside Chrome for each client website.
